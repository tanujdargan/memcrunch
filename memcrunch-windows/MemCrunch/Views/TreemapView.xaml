<UserControl x:Class="MemCrunch.Views.TreemapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BgDark}">
    <Grid>
        <Canvas x:Name="TreemapCanvas" ClipToBounds="True"
                MouseMove="Canvas_MouseMove" MouseLeave="Canvas_MouseLeave"
                MouseLeftButtonDown="Canvas_Click"
                SizeChanged="Canvas_SizeChanged" />

        <!-- Tooltip -->
        <Border x:Name="TooltipPanel" Visibility="Collapsed" IsHitTestVisible="False"
                Background="#E0111827" CornerRadius="8" Padding="8,6"
                BorderBrush="#30FFFFFF" BorderThickness="1" MaxWidth="200">
            <StackPanel>
                <TextBlock x:Name="TipName" FontSize="11" FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimary}" TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="TipSize" FontSize="10" Foreground="{StaticResource TextSecondary}" />
                <TextBlock x:Name="TipExt" FontSize="10" Foreground="{StaticResource TextTertiary}" />
                <TextBlock x:Name="TipHint" FontSize="10" Foreground="{StaticResource TextTertiary}" FontStyle="Italic" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
